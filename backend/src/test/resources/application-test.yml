spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/radiance_wellness_test
    username: postgres
    password: "0000"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: test-jwt-secret-key-for-unit-tests-only-2026
  access-token-expiration: 900000
  refresh-token-expiration: 604800000
